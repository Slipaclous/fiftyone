{% extends 'form_div_layout.html.twig' %}

{% block form_row %}
  <div class="my-custom-class">
    {{ form_label(form) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}
  </div>
{% endblock %}
